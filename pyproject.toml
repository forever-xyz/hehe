[project]
name = "hehe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "forever", email = "ikunstar520@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
    "typer>=0.27.1",
]

[project.scripts]
hehe = "hehe.cli:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
